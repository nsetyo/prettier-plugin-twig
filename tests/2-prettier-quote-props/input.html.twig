{% set sortables = {
                        code: "code",
                        "name.keyword": "Name"
                    } %}

                    {% set sortables = {
                        code: "code",
                        "keyword": "Key"
                    } %}